<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:DeviceTunerMAUI.Pages"      
       x:Class="DeviceTunerMAUI.AppShell"
       >

    <FlyoutItem Title="Switches">
        <ShellContent Route="SwitchesPage" 
                      ContentTemplate="{DataTemplate pages:SwitchesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="RS485">
        <ShellContent Route="activityindicator" 
                      ContentTemplate="{DataTemplate pages:Rs485Page}" />
    </FlyoutItem>
    
</Shell>
